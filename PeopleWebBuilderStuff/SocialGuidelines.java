/**
 * This documents different values of social guidlines people can follow
 */
public enum SocialGuidelines {
	
	/**
	 * A person doesn't follow them
	 */
	doesntFollow,
	
	/**
	 * A person follows them to a degree
	 */
	kindOfFollows,
	
	/**
	 * A person fully follows them
	 */
	follows
}
